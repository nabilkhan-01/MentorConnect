# Required
# Postgres connection string. For hosted providers (Neon/Supabase), it often includes SSL options.
# Example (DO NOT use real credentials):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL=

# Required: session signing secret (use a long random string)
SESSION_SECRET=

# Optional
PORT=3000
NODE_ENV=development

# Read-only mode
# - Server: blocks all POST/PUT/PATCH/DELETE under /api (except /api/login and /api/logout)
# - Client: blocks apiRequest() writes (except /api/login and /api/logout)
# Set to 0/false to allow edits.
READ_ONLY_MODE=1
VITE_READ_ONLY_MODE=1

# Demo seed passwords (used by `npm run db:seed:reset`)
DEMO_ADMIN_PASSWORD=admin123
DEMO_MENTOR_PASSWORD=mentor123
DEMO_MENTEE_PASSWORD=mentee123

# When true, seed will wipe demo DB tables before inserting
DEMO_SEED_RESET=1
